import { StyleSheet } from "react-native";

export const header = StyleSheet.create({
    main: {
        textAlign: "center",
        fontSize: 48,
        fontWeight: "bold"
    },
    sub: {
        fontSize: 36,
        textAlign: "center"
    }
})

